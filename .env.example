# -----------------------------------------------------------------------------
# REQUIRED: MINECRAFT SERVER SETTINGS
# -----------------------------------------------------------------------------
# The address of the Minecraft server you want the bot to connect to.
MC_SERVER_ADDRESS=your_server.com
# The port of the Minecraft server. Default is 25565.
MC_SERVER_PORT=25565
# (Optional) The Minecraft version of the server. If left blank, it will be auto-detected.
MC_VERSION=

# -----------------------------------------------------------------------------
# REQUIRED: BOT ACCOUNT & AUTHENTICATION
# -----------------------------------------------------------------------------
# Choose the authentication method.
#   'offline'  -> For "cracked" servers. You must provide MC_USERNAME.
#   'microsoft'-> For premium servers. You must provide MICROSOFT_EMAIL.
AUTH_METHOD=offline

# -- SETTINGS FOR 'offline' (cracked) ACCOUNTS --
# The username for the bot on an offline-mode server.
MC_USERNAME=BotUsername
# The password used for the server's chat-based login plugin (e.g., /login <password>).
# IMPORTANT: This is NOT your Microsoft account password. Only for offline/cracked servers.
MC_PASSWORD=your_in_game_password

# -- SETTINGS FOR 'microsoft' (premium) ACCOUNTS --
# The email address associated with your Microsoft/Minecraft account.
# The first time you run the bot, a browser window will open for you to sign in.
# Your password is never stored here.
MICROSOFT_EMAIL=your_email@example.com

# -----------------------------------------------------------------------------
# OPTIONAL: BOT & INTEGRATION SETTINGS
# -----------------------------------------------------------------------------
# A comma-separated list of Minecraft usernames that have admin privileges for in-game commands.
ADMIN_USERNAMES=admin1,admin2

# Your Discord bot's token for sending notifications.
DISCORD_BOT_TOKEN=
# The ID of the Discord channel where the bot should send messages.
DISCORD_CHANNEL_ID=

# Your ngrok authentication token.
# What is ngrok? It's a service that creates a secure, temporary, public URL
# for the dashboard running on your local computer. This is perfect for sharing
# your dashboard with a friend or for testing without deploying to Render.
# If you provide a token, a public URL will be printed in the console when the bot starts.
NGROK_AUTH_TOKEN=

# -----------------------------------------------------------------------------
# ADVANCED: INTERNAL PORT & TIMEOUT SETTINGS
# You generally do not need to change these.
# -----------------------------------------------------------------------------
# The main port for the unified dashboard. Render will set this automatically if deployed.
PORT=8080
# The internal port for the 3D viewer plugin.
VIEWER_PORT=3001
# The internal port for the web inventory plugin.
INVENTORY_PORT=3002
# The timeout in milliseconds for validating a proxy.
PROXY_VALIDATION_TIMEOUT=20000
