# -----------------------------------------------------------------------------
# Development Environment Configuration
# -----------------------------------------------------------------------------
# To run the bot in developer mode, copy this file to a new file named `.env`
# and fill in the required values below.
#
# When these core bot settings are present in `.env`, the application will
# bypass the web-based setup wizard and start the bot immediately.
# -----------------------------------------------------------------------------

# --- Minecraft Server Connection ---
SERVER_ADDRESS=your_server_ip
SERVER_PORT=25565
# Optional: Specify a Minecraft version (e.g., 1.18.2). Leave blank for auto-detection.
SERVER_VERSION=

# --- Bot Authentication ---
# Auth method can be 'microsoft' (for premium servers) or 'offline' (for cracked servers).
AUTH_METHOD=microsoft
# If using 'microsoft', provide the email address. The login flow will open in your browser.
MICROSOFT_EMAIL=your_email@example.com
# If using 'offline', provide the desired bot username.
BOT_USERNAME=Player
# If the server has an auth plugin (e.g., /login <password>), provide the password here.
SERVER_PASSWORD=your_server_password

# --- Bot Management ---
# A comma-separated list of Minecraft usernames that are allowed to control the bot in-game.
ADMIN_USERNAMES=user1,user2

# --- Proxy Settings ---
# Enable or disable the use of a SOCKS5 proxy from proxies.txt.
USE_PROXY=false

# --- Discord Integration (Optional) ---
# Your Discord bot's token. This is required to enable any Discord features.
DISCORD_TOKEN=your_discord_bot_token
# The ID of the Discord channel where the bot should send messages.
DISCORD_CHANNEL_ID=your_discord_channel_id

# --- Web Dashboard Ports ---
# These ports are for the main dashboard and the integrated plugins.
# Make sure they are not already in use on your system.
MAIN_DASHBOARD_PORT=8080
VIEWER_PORT=3001
INVENTORY_PORT=3002
