# -----------------------------------------------------------------------------
# Development Environment Configuration
# -----------------------------------------------------------------------------
# To run the bot in developer mode, copy this file to a new file named `.env`
# and fill in the required values below. When these settings are present,
# the application will start the bot directly, bypassing the setup wizard.
# -----------------------------------------------------------------------------

# --- Minecraft Server Connection ---
MINECRAFT_HOST=your_server_ip
MINECRAFT_PORT=25565
MINECRAFT_VERSION=

# --- Bot Authentication ---
# Set the authentication method. Can be 'microsoft' or 'mojang'.
AUTH_METHOD=microsoft

# If using 'microsoft', provide the email associated with the Microsoft account.
MICROSOFT_EMAIL=your_email@example.com

# If using 'mojang' auth, or for offline servers, provide the bot's username.
MINECRAFT_USERNAME=Player

# If the server requires a password for login (e.g., for some offline servers).
SERVER_PASSWORD=your_server_password

# --- Bot Management ---
ADMIN_USERNAMES=user1,user2

# --- Proxy Settings ---
USE_PROXY=false

# --- Discord Integration (Optional) ---
DISCORD_TOKEN=your_discord_bot_token
DISCORD_CHANNEL_ID=your_discord_channel_id

# --- Web Dashboard Ports ---
MAIN_DASHBOARD_PORT=8080
VIEWER_PORT=3001
INVENTORY_PORT=3002

# --- Dashboard Settings ---
# Set to 'simple' to load the minimal dashboard, or 'full' for the complete UI.
DASHBOARD_MODE=full
